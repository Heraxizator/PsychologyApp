<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PsychologyApp.Presentation.Views.Tests.TestHistoryPage"
             xmlns:ui="clr-namespace:PsychologyApp.Presentation.UI.Components"
             xmlns:vm="clr-namespace:PsychologyApp.Presentation.Modules.Tests.Collection"
             xmlns:tests="clr-namespace:PsychologyApp.Presentation.Modules.Tests.Collection"
             x:DataType="vm:TestHistoryViewModel"
             Shell.NavBarIsVisible="True"
             Title="{Binding PageTitle}">
    <Grid Margin="{StaticResource LayoutPageMarginThickness}">
        <CollectionView
            IsVisible="{Binding IsDone}"
            ItemsSource="{Binding Entries}"
            SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="tests:TestHistoryEntryItem">
                    <Border Style="{StaticResource BrandCardStyle}" Margin="0,0,0,8" Padding="12">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="{Binding DateText}" Style="{StaticResource CaptionStyle}" />
                            <Label Text="{Binding SummaryText}" Style="{StaticResource BodyStyle}" />
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <Label
                    HorizontalTextAlignment="Center"
                    Style="{StaticResource BodyStyle}"
                    Text="{Binding EmptyText}" />
            </CollectionView.EmptyView>
        </CollectionView>

        <ui:ProgressBarView
            VerticalOptions="Center"
            HorizontalOptions="Center"
            IsVisible="{Binding IsLoadingOverlayVisible}"
            IsLoading="{Binding IsLoadingOverlayVisible}"
            ProgressText="{Binding LoadingText}" />

        <ui:RetryView
            VerticalOptions="Center"
            HorizontalOptions="Center"
            IsVisible="{Binding IsFail}"
            FailedText="{Binding FailedText}"
            RetryText="{Binding RetryText}"
            RetryCommand="{Binding Reload}" />
    </Grid>
</ContentPage>
