<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PsychologyApp.Presentation.Widgets.Profile.MoodTrendChartView"
             x:Name="Root">

    <Border Style="{StaticResource TestFlatCardStyle}" Margin="0">
        <VerticalStackLayout Spacing="8">
            <Label
                Text="{Binding Title, Source={x:Reference Root}}"
                Style="{StaticResource SectionTitleStyle}" />
            <Label
                Text="{Binding Subtitle, Source={x:Reference Root}}"
                IsVisible="{Binding HasSubtitle, Source={x:Reference Root}}"
                Style="{StaticResource CaptionStyle}"
                TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}" />
            <GraphicsView
                x:Name="ChartView"
                HeightRequest="192"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </Border>

</ContentView>
