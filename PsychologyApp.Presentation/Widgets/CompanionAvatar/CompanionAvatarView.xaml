<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ui="clr-namespace:PsychologyApp.Presentation.Shared.UI.Components"
             x:Class="PsychologyApp.Presentation.Widgets.CompanionAvatar.CompanionAvatarView">

    <!--
        The same icon-tile language used everywhere else in the app (a Material icon on a tinted round tile), just on
        the companion's own brand gradient instead of a flat tint, with a slow breathing ring behind it and an online
        dot in the corner. Consistent, not another one-off avatar concept.
    -->
    <Grid>
        <Ellipse x:Name="Halo" Opacity="0" Scale="1">
            <Ellipse.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="{DynamicResource Primary}" Offset="0.0" />
                    <GradientStop Color="#6E56FF" Offset="1.0" />
                </LinearGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <Ellipse>
            <Ellipse.Fill>
                <LinearGradientBrush StartPoint="0.1,0" EndPoint="0.9,1">
                    <GradientStop Color="#6FBBFF" Offset="0.0" />
                    <GradientStop Color="{DynamicResource Primary}" Offset="0.55" />
                    <GradientStop Color="#6E56FF" Offset="1.0" />
                </LinearGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <ui:MaterialIconView
            x:Name="Icon"
            IconName="Psychology"
            IconColor="White"
            HorizontalOptions="Center"
            VerticalOptions="Center" />

        <Border
            x:Name="StatusBadge"
            WidthRequest="15"
            HeightRequest="15"
            Margin="0,0,-1,-1"
            HorizontalOptions="End"
            VerticalOptions="End"
            StrokeThickness="2.4"
            Stroke="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}"
            BackgroundColor="#34C759"
            StrokeShape="RoundRectangle 8" />
    </Grid>
</ContentView>
