<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
             x:Class="PsychologyApp.Presentation.UI.Components.MaterialIconView"
             x:Name="Root">

    <mi:MauiIcon
        x:Name="Icon"
        IconColor="{Binding IconColor, Source={x:Reference Root}, FallbackValue={DynamicResource Primary}}"
        IconSize="{Binding IconSize, Source={x:Reference Root}}"
        HorizontalOptions="Center"
        VerticalOptions="Center" />

</ContentView>
