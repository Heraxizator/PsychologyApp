<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PsychologyApp.Presentation.Views.Practice.Techniques.TheoryPage"
             xmlns:vm="clr-namespace:PsychologyApp.Presentation.ViewModels.Practice.Techniques"
             xmlns:ui="clr-namespace:PsychologyApp.Presentation.UI.Components"
             xmlns:behaviors="clr-namespace:PsychologyApp.Presentation.Common.Behaviors"
             x:DataType="vm:TheoryViewModel"
             Shell.NavBarIsVisible="False"
             Title="{Binding PageTitle}">

    <ScrollView VerticalScrollBarVisibility="Never">
        <StackLayout Margin="20,0,20,20" Spacing="20">
            <StackLayout.Behaviors>
                <behaviors:StaggerChildrenBehavior />
            </StackLayout.Behaviors>
            <ui:NavigationBarSimpleView
                TitleText="{Binding PageTitle}"
                BackText="{Binding BackText}"
                BackCommand="{Binding Finish}" />
            <ui:TextBoxView
                HeaderText="{Binding PageTitle}"
                BodyText="{Binding Text}" />
        </StackLayout>
    </ScrollView>
</ContentPage>
