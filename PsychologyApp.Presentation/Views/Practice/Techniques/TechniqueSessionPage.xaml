<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PsychologyApp.Presentation.Views.Practice.Techniques.TechniqueSessionPage"
             xmlns:controls="clr-namespace:PsychologyApp.Presentation.Controls"
             xmlns:behaviors="clr-namespace:PsychologyApp.Presentation.Common.Behaviors"
             Shell.NavBarIsVisible="False"
             Title="{Binding TechniquePageTitle}">

    <ContentPage.Behaviors>
        <behaviors:TechniqueSessionBehavior />
    </ContentPage.Behaviors>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{Binding TheoryToolbarText}" Command="{Binding Theory}" />
    </ContentPage.ToolbarItems>

    <controls:TechniquePageShell
        x:Name="SessionShell"
        TitleText="{Binding PageName}"
        Algorithm="{Binding Algorithm}"
        BackCommand="{Binding BackCommand}"
        TheoryCommand="{Binding Theory}"
        FinishCommand="{Binding CompleteCommand}">
        <controls:TechniquePageShell.Behaviors>
            <behaviors:RevealOnAppearingBehavior />
        </controls:TechniquePageShell.Behaviors>
    </controls:TechniquePageShell>
</ContentPage>
